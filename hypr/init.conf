#executa no inicio

#area de transferencia
exec-once = wl-paste --type text --watch cliphist store # Stores only text data & wl-paste --type image --watch cliphist store # Stores only image data

#autenticador, wallpaper e barra
exec-once=/usr/lib/polkit-kde-authentication-agent-1 & wpaperd -d & waybar

#apps
$changer = sh ~/.config/confchanger/confchanger
$terminal = kitty
$explorer = nemo
$browser = librewolf